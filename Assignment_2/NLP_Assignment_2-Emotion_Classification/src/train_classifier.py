
def train_classifier(config, loss_fn, optimizer):
    """
    Parameters
    ----------
    config : dict
        A dictionary that contains all hyperparameters
    loss_fn :  torch.nn.modules.loss._WeightedLoss
    optimizer : torch.optim.Optimizer

    Returns
    -------
    tuple :
        A tuple of training and loss history
    """
    print(f"Dummy training Start")
    print(f"Dummy training complete")
    return None, None
